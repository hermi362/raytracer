#pragma once

// return true if f1 and f2 are within a certain tolerance
bool isEqualEnough(float f1, float f2);
float min(float a, float b);

static const float PI = 3.1415927;
static const float ROOT2 = 1.41421356;  // sqrt(2.0)
